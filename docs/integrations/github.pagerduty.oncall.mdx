---
slug: github-pagerduty-oncall
title: "GitHub → PagerDuty: Show who's on‑call"
subtitle: See the current on‑call responder for this repository
tags: [github, pagerduty, oncall]
icons: [github, pagerduty]
keywords: [github pagerduty integration, on-call information, github pagerduty, incident management, team coordination]
description: Connect GitHub repositories with PagerDuty on-call information. See who's currently on-call for each repository to improve incident response and team coordination.
slides:
  - image: /img/slide-github.png
    alt: AWS Lambda
    url: "https://github.com/mycompany/webshop"
    targetSpanIndex: 1
    description: See who’s on‑call for this repo’s service right now
    data:
      - type: link
        content: "Oncall: John"
        href: '#'
        icon: pagerduty
      - type: text
        content: "Other integrations: ..."
  - image: /img/slide-pagerduty.png
    alt: PagerDuty
    url: https://pagerduty.com/oncall/service/shop
    description: Here's the PagerDuty on‑call view for this service
    isLastSlide: true
    data:
      - type: text
        content: "Other integrations: ..."
---

import CodeBlock from '@theme/CodeBlock';
import IntegrationCode from '!!raw-loader!@site/../examples/data/github.pagerduty.oncall.integration.example.js';
import BarContentPreview from '@site/src/components/BarContentPreview';
import SlideshowWrapper from '@site/src/components/SlideshowWrapper';

<SlideshowWrapper slides={frontMatter.slides} />

## How does this help you?

* See who is currently on‑call for this repository
* Jump directly to PagerDuty’s On‑Call view filtered to the service

## Preview

<BarContentPreview data={[
    {
      type: 'link',
      content: 'Oncall: John',
      href: '#',
      icon: 'pagerduty'
    }
  ]} title="How it looks in the extension" />

## High level approach
We assume PagerDuty services are tagged with the repository name (tag key `repository`, value matching the GitHub repo, e.g. `company/webshop`). We locate the first matching service, resolve its escalation policy, fetch the current L1 on‑call user, and show their first name.

## Prerequisites
For the code below to work, please follow the guide on [Connecting to PagerDuty](/docs/recipes/connecting-to-pagerduty). Provide `PAGERDUTY_API_TOKEN` as a secret.

## Code
<CodeBlock language="javascript" title="github.pagerduty.oncall.integration.example.js">
{IntegrationCode}
</CodeBlock>
